# Security Policy

> 本文件按 v2.8 §11 SEC-060"SECURITY.md 随仓库发布（M3）"落盘（WP-12）；三要素（联系渠道/响应承诺/版本支持面）为卡边界给定，节结构 [自定]（标准 disclosure 惯例形制）。遥测（SEC-050）默认关、opt-in、可一键关——本政策不涉及任何默认数据上报。

## Supported Versions（版本支持面）

| 版本 | 支持状态 |
| :-- | :-- |
| main（开发线，M0–M3 里程碑） | ✅ 接受报告并修复 |
| tag M0 / M1 / M2（历史冻结树） | ⛔ 不再接收安全修复（仅归档） |

## Reporting a Vulnerability（联系渠道）

- **渠道**：GitHub Issues，标题前缀 `[security]`（项目无私有漏洞披露通道，[自定]——发布工程 M5 建立正式渠道时更新本节）。
- **请附**：影响面描述、复现步骤（含命令/环境）、涉及版本（commit hash）。
- **请勿**：在公开 Issue 中直接贴可利用的完整攻击载荷。

## Response Commitment（响应承诺）

1. **确认**：收到报告后 5 个工作日内回执（[自定] 承诺值）。
2. **处置**：按 §11 SEC 章节对号入座（SEC-010~080），高危（数据外传/提权/供应链面）优先。
3. **披露**：修复合入 main 后在 Issue 内公布；在修复发布前可应报告者要求暂缓细节公开（负责任披露 [自定]）。

## Scope Notes（与规格 SEC 章的对应）

- **SEC-080**：工具子进程 env 白名单清洗——API key 绝不进入工具子进程 env；超限输出落盘文件含会话输出原文，注意其所在盘的用户可见性。
- **SEC-070**：项目级 agent 定义为供应链攻击面——`.standardcode/agents/*.md` 在工作区信任确认（UI-061）通过前不加载；提权字段（permissionMode/hooks）需二次确认并留痕。
- **SEC-040**：产物 checksum/签名（Q-7）随 M5 发布工程交付，当前阶段不承诺构建产物校验面。
- **遥测**：默认零上报（SEC-050）；ENG-090 事件基线仅定义、无默认收集实现。
