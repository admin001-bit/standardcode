# M2 上下文工程 — DoD 勾验表

> 复制自 `D:\StandardCode\StandardCode_v2.8.md` §2（2026-09-07，WP-07 迁移，逐字保留，原文为准）。

| M | 目标 | 范围（对应条目） | DoD（唯一验收标准，B-09） |
| :-- | :-- | :-- | :-- |
| **M2 上下文工程** | 前缀稳定 + AutoCompact | context-store 全套（CTX-030~038，含压缩期禁工具与"安全相关指令逐字保留"不变量）、思维链保护（CTX-020/021：thinking signature 原样回传、压缩继承 thinking 配置）、`/new` `/resume` `/reload` `/rename` `/compact` `/context` `/diff` `/rewind` `/config` `/provider` `/doctor` `/cd` `/add-dir`（/context 与真实 usage 对账）、settings 五来源合并序（§7.7）+ managed 落盘路径（Q-3）+ 信任对话框（§8.3 细则）、记忆用户编写轨（AGENTS.md/rules/@import）、transcripts 落盘（schema 见 §13、路径 `~/.standardcode/projects/<encoded>/transcripts/`）、权限持久化（"总是允许"只落 local 层） | 压缩阈值表测全过；20+ 轮长会话缓存命中率达标（目标值 M1 live 校准后定）；E2E 场景 ①（以"无后台任务时的等价断言"执行）通过；开发者本人连续 5 个工作日真实使用无阻塞性缺陷 |

## 勾验表

- [x] 压缩阈值表测全过（WP-03 V 核验 5/5+阈值表测入 CI test 门全量运行——WP-13，2026-09-09；packages/context/test/autocompact.test.ts 12 用例含 CTX-033 表测+pct 边界+DoD⑤ 手动窗边界）
- [x] 20+ 轮长会话缓存命中率达标（BLK-02 解阻塞：用户提供 key→subagent 22 轮 live 实测（产品真实装配，glm-5.3-flash 网关）**overall 86.1% / stable（第 3 轮起聚合）88.5% / turn1 cold 0%**；目标值=stable hit ≥80% [自定]（X 代拟，BLK-01 终裁，G 门用户终确认）；观察：turn3 网关逐出单次抖动、cache_creation 恒 0=网关隐式缓存模式——WP-13，2026-09-09；测量脚本+逐轮数据 .work/wp13-hitrate/）
- [x] E2E 场景 ①（以"无后台任务时的等价断言"执行）通过（apps/cli/test/e2e-scenario1.test.ts：22 文件真实 Write 重构+中途真实 npm install（本地 file: 零网络）+无装依赖对照组逐文件等价+无悬空 tool_use 终态核对——WP-13，2026-09-09）
- [ ] 开发者本人连续 5 个工作日真实使用无阻塞性缺陷（使用记录随真实使用登记中；最终勾验于 G 门审由用户确认 [自定]——卡边界原文）
