# M5 生产加固 — DoD 勾验表

> 复制自 `D:\StandardCode\StandardCode_v2.8.md` §2（2026-09-07，WP-07 迁移，逐字保留，原文为准）。

| M | 目标 | 范围（对应条目） | DoD（唯一验收标准，B-09） |
| :-- | :-- | :-- | :-- |
| **M5 生产加固** | 企业级成色 | 沙箱后端（Rust，EXE-011）、SEC 全章落地、遥测 opt-in、发布工程（附录 E）、evals live 门禁 | 三平台安装/更新/卸载脚本全过；SEC 威胁表逐行有对策证据 |

## 勾验表

- [x] 三平台安装/更新/卸载脚本全过 — 载体=**WP-07**（5328374+6f035c8，V 核销 DoD①-⑧ 全 ✓）：三平台安装脚本（install.sh/ps1）+`standardcode uninstall [--purge]`+手动清理脚本（cleanup.sh/ps1）+npm 通道+checksum（SEC-040）+更新原子性（ADR-0045 三层：锁错重试/装后校验/明确引导）；**实跑证据**：Windows 本机+WSL2 Linux+CI release-matrix 35193199123（macOS 通道）三平台「装→--version→更→卸→残留 gone→隔离 purge PURGED-OK」全链；**DoD⑦ 真实 registry 四步全链闭**（2026-09-17 用户授权 publish `@standardcode-oss/cli@0.1.0`→查询 200/真实安装/更新路 runNpmUpdateAtomic `ok:true verified:true`/卸载 gone；包名改判=ADR-0044 决策 10）。
- [x] SEC 威胁表逐行有对策证据 — 载体=**WP-05**（6494fd2+勘误 4adba6d，V 核销 DoD①-⑥ 全 ✓）：`docs/dev/sec-threat-evidence.md`=S-1~S-10 逐行三列（对策条目/实现指针/测试指针）+SEC-010~080 九条对账附表+审计遍记录；实缺四件清偿（SEC-020b settings env 注入键黑名单/SEC-030 疑似密钥告警/SEC-030 keychain 优先级链/S-9 逐条强制确认含"Auto Mode 不豁免"）。

## CI 对账记录（gh run list/view，2026-09-17，WP-10 逐项）

M5 全部 **25 commit**（`git rev-list --count M4..HEAD`=25 实测；M4 冻结 tag=e612002，不含本收口 commit）→ run 全枚举对账：

| 卡 | commit → CI run | 结论 |
| :-- | :-- | :-- |
| WP-01 | e4d5d6d → 34969535833；387b126 → 34975379862；9ed75c5 → 34976219873 | success ×3（V 退回 R1 net-allow 形→修复→复验核销） |
| WP-02 | 13f1cd5 → 35044817114；fbcc1fb → 35045403431；025e5b5 → 35045872720；950a6e5 → 35046499489；62704de → 35051971812；5ebf582 → 35052619982；1e52aba → 35060607255 | failure ×3（macOS 三轮真跑：伪通配动词→顶层别名→-D 值形）→ success；**failure（62704de=R1 门禁 panic，实证 GH runner 亦无 SeAssignPrimaryToken=设计生效非回归）**→ success ×2（BLK-04=① 三段形终态） |
| WP-03 | 2e5f631 → 35072520376；84570fd → 35073188446；6320004 → 35073981710；4eb0884 → 35074812022 | failure ×3（ubuntu bwrap 缺位→AppArmor userns 拒→128KB argv E2BIG，三连收敛）→ success |
| docs（仓务） | e17b2e5 → 35090832836 | success |
| WP-04 | 38f9738 → 35123773982 | success |
| WP-05 | 6494fd2 → 35131882913；4adba6d → 35140860633 | success ×2（后者=勘误 docs-only） |
| WP-06 | 7c639d9 → 35179729290 | success |
| WP-07 | 5328374 → 35192735707（ci）+35192738096（release-matrix 首跑 **failure**：windows 红步=bash 否定语法 ParserError）；6f035c8 → 35193184624（ci）+35193199123（release-matrix 三平台） | 首跑 windows 红步→shell 标注修复→success ×2（矩阵三平台全 success） |
| WP-08 | 811fca7 → 35204605543 | success |
| 包名改判（发布事件） | 168283f（同批推送，覆盖 run=35222390737）；b7ddcaf → 35222390737 | success（ADR-0044 决策 10：scope `standardcode` 被占→`@standardcode-oss/cli`） |
| WP-09 | c2eef69 → 35239925255 | success |

**HEAD（c2eef69）run 35239925255 步骤级逐项**（gh run view --json jobs 实测）：gate job（ubuntu/macos/windows）三平台 conclusion=success；每 job 内 **install / install ripgrep / version gate / cold-start gate / typecheck gate / rust build / test gate / rust fmt gate / rust clippy gate / rust test gate 十步全 success**（`verify bwrap usable` 为 Linux 条件步：ubuntu=success、Windows/macOS=skipped）。即 **TS 泳道（typecheck+test）+ rust 泳道（fmt+clippy+test）双泳道三平台全绿**。

## 其余收口项指针（DoD③④⑤⑥，明细在 M5-1-results §WP-10）

- **DoD③ 接缝登记**：`docs/dev/seams.md` 追加 **③（沙箱落地更新）+⑭+⑮** 三条，各=定义+锚点+测试三列（追加式，前 59 行基线逐字节验证无损、既有行零触碰）。**第⑯条接缝缺定义面→阻断开出 BLK-05**（卡目标/交付物/DoD③ 记"③+⑭⑮⑯ 四更新"，而本板接缝自检行仅定义 ③⑭⑮ 三条，P 流水"新增⑭⑮⑯"之 ⑯ 无定义/锚点/测试面——规格空白不会话内裁决，protocol §5/§7）。
- **DoD④ ADR 在位与复审落档**：`docs/adr/0044-release-channels-and-uninstall.md`（BLK-02=① 通道集+postinstall 零提权+uninstall 语义+checksum 形状+**Q-5 维持 npm 先行/Q-6 bun 不引入/Q-7 首版不签**+发布授权停点+**决策 10 包名改判**）与 `0045-update-atomicity.md`（§13 清偿：锁错重试/装后校验/明确引导+非锁 fail-fast）两件在位亲验；M5 无新增 ADR 编号需求（新决策均落 0044 决策位）。
- **DoD⑤ 命令全集 30 零增量**：`apps/cli/test/wp10-milestone.test.ts` 3 例=①注册表名集合 ≡ §8.2 M1∪M2∪M3∪M4 且恰 30 件②postinstall 零提权负查（`apps/cli/package.json` scripts 无 pre/post/install 生命周期脚本）③M5 新增面（sdb/uninstall/sandbox）零注册=斜杠零新增（`-sdb` 旗标/`uninstall` 子命令面不进斜杠集合）。与 M4 `wp12-command-set.test.ts`（集合相等）同源互补（本卡=收口时点零增量复核）。
- **DoD⑥ 偏差汇总遍与全量**：程序化计数（python 枚举带圈序号，2026-09-17）：M5 九卡偏差登记 **75 条**（按 WP-01→09 序 10/10/10/6/8/9/8/8/6），与逐卡板卡/结果页声称数 **一致 9/9**；`[自定]` 标注出现数=**45**（同序 10/1/5/5/11/7/1/2/3，限定交付物/过程要点/偏差登记三类条目口径）供 G。全量套件与八包 tsc 0 实测见结果页 §WP-10 测试输出尾部。

## M4 卫生 34 项顺带处理表（逐条做/不做留痕；卡边界=顺带义务非 DoD）

> 来源=M4-1-results §G 移交清单（行 270）逐项；摘要引自 M4 各卡核验/复验节原样。处置口径：M5 仅在既有卡已顺带清偿时记"已做（卡号）"，其余记"不做"+理由（B-03 不提前实现/非 M5 范围/无害观察/工作区侧勘误题）。

| # | 来源（M4 O 项） | 摘要 | 处置 | 理由 |
| :-- | :-- | :-- | :-- | :-- |
| 1 | WP-02 O2 | mcpReady fire-and-forget 无诊断位 | 不做 | M5 无 MCP 变更面（B-03） |
| 2 | WP-02 O3 | mcpConnections 暂无消费方（WP-03 留位） | 不做 | 同上（WP-03 已消费则闭合，M5 未触及） |
| 3 | WP-02 O4 | "0s" 阈值换算可接受（[CC] 同 round） | 不做 | 观察级，既有无害 |
| 4 | WP-02 O5 | list_changed 监听器累积隐患（刷新重订阅） | 不做 | 低频无害 |
| 5 | WP-02 O6 | M4 档案无独立摘要（编号位在 O2-O7 序列内） | 不做（面不可定位） | 诚实登记：档案缺摘要行，留 G 知悉 |
| 6 | WP-02 O7 | 翻转前服务端错误路径 reject 时 forwardAbort once 监听器滞留 | 不做 | 功能无碍；M5 不新增实现（卡边界） |
| 7 | WP-03 O1 | url 留痕（http/sse 分支）无测试断言（夹具全 stdio） | 不做 | 夹具限制，非 M5 范围 |
| 8 | WP-03 O2 | "插值前 raw"未差分钉死 | 不做 | 同上 |
| 9 | WP-03 O4 | approve-on-disabled 回显 hint 未覆盖 | 不做 | fail-closed 方向，无害 |
| 10 | WP-04 O2 | decisionReason.hookName 形状异 [CC]（:61770） | 不做 | [自定] 形制已核销 |
| 11 | WP-04 O3 | 五来源合并 projectLocal/projectShared 循环路径无直测 | 不做 | M4 核销面，M5 无合并序变更 |
| 12 | WP-04 O4 | disableAllHooks=任一来源 true 即关（OR 方向无测试钉边界） | **已做（M5 WP-04）** | 安全首三件清偿：agent 级引擎并入会话总闸 OR 形+三组 before/after 判别断言 |
| 13 | WP-04 O5 | detectProjectWorkspace 8.3 短名归一缺口（context=M2 冻结面） | 不做 | 冻结面，不越卡 |
| 14 | WP-04 O6 | 超时即决后孤儿进程不回收（与 WP-02 O7 同族） | 不做 | 无害，M5 无 hooks 超时变更 |
| 15 | WP-05 O2 | user-invocable 解析未消费 | 不做 | 非 M5 范围 |
| 16 | WP-05 O3 | 同内容省略文案形状偏离未登记 | 不做 | 文案级 |
| 17 | WP-05 O6 | name-only 行仍计入预算（超支保底 [自定] 未登记） | 不做 | 保底无害 |
| 18 | WP-05 O7 | 会话中途获信任不触发 skills 重载 | 不做 | 保守方向 |
| 19 | WP-05 O8 | sourceRank 去重优先序未入偏差登记 | 不做 | 登记级 |
| 20 | WP-06 O2 | SessionLock memory 目录生产取锁点 | 不做 | M5 未触及 memory 生产取锁（WP-06 遥测卡边界） |
| 21 | WP-09 O2 | 插件 hooks 相对路径无 pluginRoot 留痕/语义未对齐 | 不做（留 G 定夺） | M5 无 plugin hooks 变更面 |
| 22 | WP-09 O4 | git clone 网络副作用先于对话框（落地面零落地已成立） | 不做 | M5 WP-08 清偿的是**认证/代理/脱敏**面，非"副作用前置"顺序面（M4 档案判落地面已成立） |
| 23 | WP-09 O5 | 同名 destDir 无记录时覆盖 fail-open 微面 | 不做 | M5 未触及 |
| 24 | WP-09 O6 | marketplace git URL 添加路共用代码无直测 | 不做 | M5 WP-08 仅补 clone 路（认证/代理），添加路未触及 |
| 25 | WP-10 O2 | agent 引擎 loadHookConfigs 告警丢弃（坏形静默 no-op） | 不做 | M5 WP-04 清偿的是 M4 WP-10 **O1**（disableAllHooks agent 级旁路），O2 告警面未触及 |
| 26 | WP-10 O3 | EN catalog subtask 文案嵌全角【WP-10】 | 不做 | 文案级（i18n 中英混排=用户侧策略项，仪表盘 3.1 在案） |
| 27 | WP-10 O4 | bare Agent 全禁形不提取（[CC] 形制内） | 不做 | [CC] 形制内，无害 |
| 28 | WP-10 O5 | drainSpill once 滞留 | 不做 | 无害（同 WP-02 O7 族） |
| 29 | WP-11 O2 | renderReport v1 题头硬编码（"M5 v2 时改派生式"） | **已做（M5 WP-09）** | runner `lineageFor(version)` 派生式清偿，v0/v1 系谱串逐字保留=存档不回改 |
| 30 | WP-11 O3 | direct destructiveOps 恒 0 语义遗留（=偏差⑤，live=M5 载体） | **部分已做（M5 WP-09）** | live 抽样族 l2/l3 destructiveOps 由真实 `tool_use` 派生命中数（载体面清偿）；**direct 族自报语义维持**（M4 注记②口径=语义本正非虚位） |
| 31 | WP-11 O4 | b19 usage 手抄常量 margin 小（textProvider 形状改动需同步） | 不做 | 夹具常量，当前零漂移 |
| 32 | WP-11 O6 | afterAll catch 吞清理失败（tmp 残留微面） | 不做 | 无害（系统 tmp 自清） |
| 33 | 行号漂移族=WP-01 O3 | dig-05 引用行号与 minified 物理行 1-3 行漂移（内容全真） | 不做 | 工作区侧报告勘误题，归根 README §5 批量待办（非产品仓面） |
| 34 | 行号漂移族=WP-03 O5 | teardown `names instanceof Set` 恒假死分支 + :154600 指针微漂（内容全真） | 不做 | 同上（与 WP-01 O3 同性质） |
