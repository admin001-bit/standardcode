# M7 长尾 — DoD 勾验表

> 复制自 `D:\StandardCode\StandardCode_v2.8.md` §2（2026-09-07，WP-07 迁移，逐字保留，原文为准）。
> **〔收口订正 2026-09-25，WP-14〕** 范围列 `Q-5~Q-9` → `Q-5–Q-9`（en dash），与 v2.8 行 115 **逐字一致**——DoD① 要求程序化逐字对质，波浪号形不通过；M6.md 同形差异属**冻结档案**，按 protocol §8.6 不回改。

| M | 目标 | 范围（对应条目） | DoD（唯一验收标准，B-09） |
| :-- | :-- | :-- | :-- |
| **M7 长尾** | 收尾 | `/goal` `/effort` 细化、主题/快捷键、OTel 导出、Q-5–Q-9 遗留裁决 | 附录 E 全通道可用 |

## 勾验表

- [x] 附录 E 全通道可用 — 载体=**WP-09（独立二进制+checksum）→WP-10（四通道包）→WP-11（更新双源+卸载）三卡全核销**（DoD 载体映射见 M7-1-results §P 开板记录）；判据逐条对附录 E 行 630 见下表 11 条。**卡 DoD①「两勾判据」措辞说明**：M7 DoD 实为**单勾**（M6 为两勾），该措辞承 M6 收口形制而来；本卡对质面=**DoD 列＋范围列两处文字**与 v2.8 行 115 程序化逐字对质（断言见 `apps/cli/test/wp14-milestone.test.ts`）——登记供 G 门知悉。

## 附录 E（v2.8 行 630）逐条分解勾验

> 判据口径：每条=「条款原文 → 载体卡 → 证据（文件:行号 / 实测）→ 结论」。结论 ✓＝通道**可用**（产物+校验+验证记录齐备）；✓*＝产物与静态验证在位、**安装/实跑链未本地验证**（BLK-12=① 口径，如实登记）。

| # | 附录 E 条款 | 载体 | 证据 | 结论 |
| :-- | :-- | :-- | :-- | :-- |
| 1 | npm（`@standardcode-oss/cli`，全平台首选；postinstall 不做提权动作） | M5 WP-07（发布工程）+M7 WP-10 DoD④ 零触碰 | `apps/cli/package.json:2` name=`@standardcode-oss/cli`（ADR-0044 决策 10 改判）；package.json **无 scripts 段**＝零生命周期脚本（断言 `apps/cli/test/wp12-milestone.test.ts:39`）；WP-10 `git diff --name-status` **空**＝npm 通道零触碰 | ✓ |
| 2 | 独立二进制（Bun compile/Node SEA，附 checksum） | **WP-09** | `scripts/build-binaries.mjs`+`apps/cli/bin/standardcode.binary.mjs`+`scripts/checksum.mjs`+`ci.yml` binaries 泳道；三平台产物 sha＝win `6ca25cfe`／linux `4fbbded3`／darwin `4b3b83cc`；实跑＝Win 本机 version+冷启动 173.3ms／WSL2 81ms／macOS 由 CI（run 35511368578 `verify darwin-arm64: pass`，冷启动 28.1ms）；checksum verify PASSED＋篡改必红 | ✓ |
| 3 | Windows winget | **WP-10** | `packaging/winget/manifests/` 三 YAML；`winget validate` **0 告警 exit=0**（545867d）；**安装/卸载链未本地验证**（需管理员启用 `LocalManifestFiles`，BLK-12=①） | ✓* |
| 4 | Windows 直接下载（PATH 需确认；签名 Q-7） | **WP-10** | `packaging/direct-download/{verify.sh,verify.ps1,README.md}`；Windows 本机正负例实跑（正例 `checksum OK`／篡改例 exit 1）；PATH 需确认=`scripts/install.sh:39` 需字面 `yes`（ENG-040 行 431）；签名=Q-7 维持不签（ADR-0046 结案） | ✓ |
| 5 | macOS Homebrew（公证随 Q-7） | **WP-10** | `packaging/homebrew/standardcode.rb`；用户态 ruby 3.3.8 `ruby -c`＝Syntax OK；**brew 安装链未本地验证**（无 macOS，BLK-12=①）；公证随 Q-7=不签 | ✓* |
| 6 | Linux 安装脚本/deb\|rpm | **WP-10** | `packaging/linux/{install.sh,uninstall.sh,build-deb.sh,build-rpm.sh,standardcode.spec}`；WSL2 **真装真卸**（deb 纯 shell ar+tar、rpm 用用户态 rpmbuild 6.0.1、经 `unshare -r` fake-root），包内 bin sha==产物、残留 bin/doc 双 NO | ✓ |
| 7 | 更新走 npm registry | **WP-11**（复验） | `packages/platform/src/updater.ts:82` `checkRegistryLatest`；真实只读探测 `HTTP 200 / latest 0.1.0`（与 package.json 同源）；原子性 ADR-0045 断言 `wp07-updater-atomic` **零改动 10/10 绿** | ✓ |
| 8 | 更新走 GitHub Releases | **WP-11**（新增） | `packages/platform/src/updater.ts:50` `checkGitHubLatest`＋`:44` `normalizeReleaseTag`；真实探测 `HTTP 404`（本仓 `releases?per_page=1`=`[]`，承 ADR-0044 决策 8 结构化降级口径）；ADR-0050 双源并列、**GitHub 源已导出未接线 /update**（未验证面⑦） | ✓ |
| 9 | 卸载 `standardcode uninstall --purge` | **WP-11** | `apps/cli/src/uninstall.ts:29`（`--purge` 解析）／`:135-193`（五步全链）；`wp11-uninstall-channels` **17/17 绿**；非 TTY 无确认通道=拒绝（fail-closed）；残留失败=purge 不执行（防半卸态） | ✓ |
| 10 | 卸载手动清理脚本 | **WP-11** | `scripts/cleanup.sh`（`--purge-home`）/`scripts/cleanup.ps1`（`-PurgeHome`）；三平台用例内**静态断言**（旗标在位＋包名与 `NPM_PACKAGE_NAME` 同字＋旧名零命中）；**实跑未验证**（本机 `spawnSync` 恒 EBUSY／`wsl.exe` 被拦截） | ✓* |
| 11 | **品牌资源** `dog-logo/`（.ts ESM／.js CJS／.png；置终端左侧产品标识位，正右方配 "StandardCode, Born for Engineering."） | — | **产品仓零命中**：`git log -S DOG_BITMAP -- . ":(exclude)docs"` 空（**排除本档自指**——本档登记该字符串后裸 `git log -S` 会命中自身）、全仓 grep `dog`／`logo`／`Born for Engineering`（排除 node_modules 与本档）零命中；三件实物仅存于工作区 `D:\StandardCode\dog-logo\` | **✗ 未清偿** |

**DoD 裁定**：附录 E 的**通道面（1–10）全部可用**（✓ 7 条＋✓* 3 条，后者为环境不可达的如实登记，非缺陷）⇒ **DoD「附录 E 全通道可用」成立，勾 ✓**。第 11 条品牌资源属附录 E **非通道条款**，其缺位不影响 DoD 成立，但为**规格面缺口**，随收口登记为遗留（见「全板遗留与未验证面汇总」#19，留 G 门裁量）。

## CI 对账记录（2026-09-25，WP-14）

- **远端 HEAD 核对**：`gh api repos/admin001-bit/standardcode/commits/main --jq .sha`＝**`3aa31e7a462a92ee9abc0c9622f0d1bdc8497358`**＝本地 HEAD——**收口四笔全部推送**（`ae7dcb6`→`6d17f20`→`1fa941c`→`3aa31e7`；排障根因＝git 默认 HTTP/2 传输挂死＋credential helper 链卡顿，修复形制＝`-c http.version=HTTP/1.1` ＋ token URL push，教训入工作区仪表盘 §3.3）。**本条为时点值，复算命令同上。**
- **M7 区间**：`git rev-list --count 069872d..HEAD`＝**33 commit**（截至 2026-09-25 复算；M6 冻结 tag `069872d` 不含）。**注意该数随收口后每笔新 commit 递增**——本表钉的是**复算时点值**，非恒值；复算命令＝`git rev-list --count 069872d..HEAD`。
- **run 全枚举**（`gh run list --limit 100`，按 headSha 归属 M7 区间）：**18 runs，conclusion 全 success，failure 0**（33 笔已推送 commit 承载；三笔 docs-only 由 run 18 同批承载）。

| # | run | headSha | jobs | 结论 |
| :-- | :-- | :-- | :-- | :-- |
| 1 | 35456999190 | 7a81b65（WP-01） | 3 | success |
| 2 | 35463473201 | c9dbc35（WP-05） | 3 | success |
| 3 | 35465295680 | edf9d14（WP-12） | 3 | success |
| 4 | 35468866426 | ce26cd4（WP-13） | 3 | success |
| 5 | 35471906376 | e2561c1（WP-09 ADR） | 3 | success |
| 6 | 35511368578 | 352fffc（WP-09） | 6 | success |
| 7 | 35780310776 | f11e3cc（WP-03） | 6 | success |
| 8 | 35784030158 | ea8af38（WP-04） | 6 | success |
| 9 | 35788831634 | 264dbec（WP-06） | 6 | success |
| 10 | 35792692493 | 2522828（WP-08） | 6 | success |
| 11 | 35794132247 | 5e081e1（seams 订正） | 6 | success |
| 12 | 35797154831 | 3c50aa6（WP-10） | 6 | success |
| 13 | 35802156572 | 545867d（WP-10 修复） | 6 | success |
| 14 | 35914205503 | 04e61cb（WP-07） | 6 | success |
| 15 | 35931845745 | f4e6444（WP-11 批次） | 6 | success |
| 16 | 35939651388 | 8dffa24（WP-11 勘误） | 6 | success |
| 17 | 36044903759 | ae7dcb6（本收口卡） | 6 | success |
| 18 | **36059927234** | **3aa31e7（收口三笔 docs-only 同批）** | 6 | success |

- **job 级逐枚**：`gh api …/actions/runs/<id>/jobs` 逐 run 取值，**5×3＋13×6＝93 个 job 实例 conclusion 全 success**（前 5 run 为 3 job＝仅 gate 三平台；`binaries` 泳道自 WP-09（run 35511368578）起并入→6 job）。**收口 run 36044903759**（`ae7dcb6`）与 **36059927234**（`3aa31e7`，docs-only 三笔同批）均六 job 全 success。
- **HEAD run 35939651388 步骤级**（`gh run view --json jobs`）：gate×3＋binaries×3 六 job；gate job 内 install／install ripgrep／version gate／cold-start gate／typecheck gate／rust build／test gate／rust fmt／clippy／rust test **十步全 success**；非 success 仅 `verify bwrap usable` 在 win/mac＝**skipped**（Linux 专属条件步，设计预期）。
- **CI flakes 类**：已知可致红形态＝macOS gate 因 vitest Unhandled Errors（`wp09-plugin-session` MCP stdio write EPIPE）判红 → 处置 `gh run rerun <id> --failed`。**本区间 18 run 零命中**（无 failure），故**未发生 rerun**；该形态留作后续卡/G 门的既知 flake 登记。
- **33 commit → 18 run 的归并**：同批推送的 commit 由该批 head commit 的 run 承载（如 run 35931845745 承载 846c44e＋9740a45＋8fffaa2＋f4e6444），gh 全枚举其 headSha 零独立 run 命中，证**无漏无多**。

## 接缝登记（DoD③）

`docs/dev/seams.md` 追加 **㉒OTel 导出 × 遥测 opt-in 门**／**㉓`/sandbox` 命令面 × M5 沙箱后端三平台臂**／**㉔主题/快捷键 × 终端渲染单源** 三条，各=定义+锚点+测试。**追加零删除基线验证**：`git diff --numstat docs/dev/seams.md`＝**+23/−2**，两处删除**全部**为本次点名订正的活文档陈旧行（接缝㉑ 定义行"门内注册枚举"与测试行），既有行零误删。同批订正 WP-07 V 报的活文档陈旧项：接缝㉑ 门内注册枚举补入 `branch`/`batch`/`loop` 与侧信道 `btw`（原文仅列 `workflows`/`fork`/`export`）。

## 断言集（DoD④）

`apps/cli/test/wp14-milestone.test.ts`（新，**9 用例／5 个 describe**，映射卡 DoD①–⑥ 六条判据，映射关系见文件头注释）：①**DoD 逐字对质**——M7.md 表行 DoD 列与范围列与 v2.8 行 115 硬编码原文逐字相等＋勾验表勾已勾 ②**命令全集守恒**＝35 且集合 ≡ §8.2 M1∪M2∪M3∪M4∪M7 ③**实验门候选面守恒**＝7 件（含 `btw` 侧信道）＋缺省关活跃放行面 `[]` ④**附录 E 通道面在位**（packaging 四通道＋scripts 六件文件级；四脚本包名守卫新名在位/旧名零命中；updater 双源导出；`--purge` 解析）⑤**接缝登记零删除基线**——seams.md 含 ㉒㉓㉔ 三条且门内注册枚举含 branch/batch/loop/btw ⑥**偏差分桶计数**——M7.md 遗留汇总表条目数分桶断言。

## 偏差汇总（DoD⑤）

> 口径承 M6 WP-12：分桶＝**A 随收口登记（本卡已订正/已闭合）／B 留 G 门／C 留后续卡**，逐条只在**唯一桶**计数。

| 桶 | 条目数 | 条目号 | 含义 |
| :-- | :-- | :-- | :-- |
| A 随收口登记 | **5** | #1–#4/#17 | 本卡已订正（4 条）或已闭合（1 条） |
| B 留 G 门 | **6** | #7/#8/#16/#19/#21/#22 | 需用户/门审裁决（规格变更候选、判据缺口、口径裁量） |
| C 留后续卡 | **12** | #5/#6/#9–#15/#18/#20/#23 | 环境不可达或待外部条件，落后续卡/CI |
| 合计 | **23** | — | 分桶互斥，逐条唯一归属 |

## 全板遗留与未验证面汇总（M7-1 板，WP-14 收口）

> 来源＝WP-07 六条遗留（M7-1-results §WP-07 V 核验节）＋WP-11 机器状态 O 两条（§WP-11 V 核验节）＋WP-11 未验证面八条（`packaging/update-uninstall-matrix.md` §4）＋本卡勾验新发现（#19）＋**既往未清偿遗留的交叉回扫**（WP-12 G-1/G-2＝#20/#5、WP-10 D-1＝#21、WP-10 BLK-12＝#18、M6 §G＝#22、M5 §G 移交＝#23，共 6 条）。处置口径承 M6 卫生表：仅在本卡已顺带清偿时记「已做（收口）」，其余按 B/C 分桶并给理由。

| # | 来源 | 摘要 | 处置 | 理由 |
| :-- | :-- | :-- | :-- | :-- |
| 1 | WP-07 遗留③ | `docs/dev/seams.md:112` 门内注册枚举未含 branch/batch/loop 与侧信道 btw（活文档陈旧） | **A 已做（收口）** | 本卡订正（+ 测试行补 wp14 指针）；零删除基线已验证 |
| 2 | WP-07 遗留⑤ | 注释陈旧两处（`wp01-experimental.test.ts:198` 行尾"放行面=三件"实为六件；`experimental-gate.ts:44`"推后 M7 四件恒不含"已过时） | **A 已做（收口）** | 纯注释订正，零行为影响，与 #1 同族（活文档陈旧） |
| 3 | WP-07 遗留④ | ADR-0049 决策 1 表未并列 `/btw→teams`（可读性） | **A 已做（收口）** | 决策 1 行内补并列注（`/btw` 为 teams 侧信道，见决策 3），不改决策内容 |
| 4 | WP-07 遗留⑥ | 本机环境致红名单待扩录（`spawnSync git EBUSY` 9 文件／node EBUSY／无 shell／bun 与 pack 不可跑／`afterAll` hook 超时） | **A 已做（收口）** | 已扩录：本表＋工作区仪表盘 §3.3（环境致红族逐项点名）；环境致因不计回归 |
| 5 | WP-12 遗留 G-2 | 根 `package.json` 与 `packages/*/package.json` 缺 `license` 字段（**仅 `apps/cli` 有**） | **C 留后续卡** | **〔订正 2026-09-25，V-A 复核〕原文义务含根与 packages/\* 两面，实测根与六包 license 全 0、仅 `apps/cli/package.json:5` 在位 ⇒ 未闭合**，原记「A 已闭合」系**错划**（仅凭 apps/cli 一面下判） |
| 6 | WP-07 遗留① | 装配层 `isInterrupted` 零测试覆盖（`main.ts:209/212-217` 注入无任何测试触及；"真 Ctrl+C 于 /loop 执行期生效"需 TTY） | **C 留后续卡** | 需 TTY/信号面端到端夹具；补装配层断言归后续卡 |
| 7 | WP-07 遗留② | teams 关侧对拦截块**结构性不可判别**（删块后走 registry 缺席分支、副作用同形＝0 红针） | **B 留 G 门** | 判据缺口已由 teams 开侧 provider 调用计数例兜底；是否补判别面由 G 裁 |
| 8 | WP-11 O-① | 本机 `spawnSync` 恒 EBUSY 使 10/13 环境红同签名，逻辑正确性只能**签名归因**背书 | **B 留 G 门** | 建议健康子进程环境复跑收窄；是否列入 CI 对账常设项由 G 裁 |
| 9 | WP-11 O-② | `wp09-plugin` `afterAll` `rmSync` 10s hook 超时（Windows 文件锁形态） | **C 留后续卡** | 卡外事项（M4 期测试文件），本机文件锁环境特有 |
| 10 | WP-11 未验证① | 真进程面零实跑（`npm i -g`/`npm rm -g`/`winget`/`brew`/`reg`） | **C 留后续卡** | 本机 spawnSync 恒 EBUSY；全链结论建立在注入面等价性上 |
| 11 | WP-11 未验证② | Linux 臂 WSL2 未实跑（`wsl.exe` 被安全策略拦截） | **C 留后续卡** | 环境策略，非缺陷；前卡 WP-10 曾可用 |
| 12 | WP-11 未验证③ | macOS 臂全格待 CI | **C 留后续卡** | 本机无 macOS；由 `release-matrix`/三平台 CI 承载 |
| 13 | WP-11 未验证④ | GitHub Releases **成功路**无真实样本（本仓 releases 为空数组） | **C 留后续卡** | 待真实 release 建立后复跑探测脚本即闭（读操作，非外发） |
| 14 | WP-11 未验证⑤ | `--purge` 删除失败分支未覆盖（需 OS 级故障注入） | **C 留后续卡** | 本机不可构造占用句柄故障 |
| 15 | WP-11 未验证⑥ | `defaultPathRestorer` win 分支（`reg query/add`）实跑 | **C 留后续卡** | 待管理员机/CI |
| 16 | WP-11 未验证⑦ | 双源**择优/回退**规格未定义；GitHub 源已导出**未接线 /update** | **B 留 G 门** | ADR-0050 决策 1 已定"不引入"；是否接线属规格变更，由 G 裁 |
| 17 | WP-11 未验证⑧ | 邻通道同族缺陷（安装脚本旧包名） | **A 已闭合** | main `8fffaa2` 订正 `install.sh`、`install.ps1` 并补守卫断言，四脚本包名一致 |
| 18 | WP-10 BLK-12 | winget 安装链／brew 安装链未本地验证（勾验 #3/#5 的 ✓*） | **C 留后续卡** | 用户裁决=① 口径：由 CI/用户机验证并如实登记；winget 需管理员启用 LocalManifestFiles |
| 19 | **本卡勾验 #11（新发现）** | 附录 E **品牌资源** `dog-logo/`（.ts/.js/.png＋终端标识位）产品仓零命中 | **B 留 G 门** | 属附录 E 非通道条款，缺位不影响 DoD 成立；是否落后续里程碑/如何入库由 G 裁 |
| 20 | WP-12 遗留 G-1 | README **未签名声明**未落地（Q-7 首版不签名的对外声明） | **C 留后续卡** | README 零命中签名/checksum 说明；同族 G-2（license 字段）亦**未闭合**（见 #5） |
| 21 | WP-10 D-1 | harness deny 段仅查显式 subagentType（缺省类型×deny 在 /fork 与 /subtask 两通道同不设防） | **B 留 G 门** | M3 既有语义，非本板引入；"缺省类型也过 deny"属规格变更 |
| 22 | M6 §G 移交 | M6 统一接线义务剩余段（②scriptPath 回填／③配方投递／⑤agent loop 四注入面／⑥per-worker 转录／⑧UI 确认路径／⑦loop 灌回） | **B 留 G 门** | BLK-10=① 已拆分转 WP-06/07；是否 form 化为下一里程碑接线卡由 G 裁 |
| 23 | M5 §G 移交①② | 提权渠道到手即 `--ignored` 补跑 CHILD-RAN；windows 沙箱臂全链·Job Object 与 server 子进程联动·elevated 路线 | **C 留后续卡** | 实机回归清单，依赖提权渠道/硬件环境 |

**小结**：**23 条**＝**A 5**（#1–#4 本卡订正＋#17 已闭合）＋**B 6**（#7/#8/#16/#19/#21/#22）＋**C 12**（#5/#6/#9/#10/#11/#12/#13/#14/#15/#18/#20/#23）。分桶互斥、逐条唯一归属；计数由 `apps/cli/test/wp14-milestone.test.ts` 按表体**逐行逐格**解析实测（不采信散文计数；格数不符即红，防裸竖线致列错位）。
> **〔订正 2026-09-25，V-A/V-B 复核〕** 本节曾有三处失准，均已就地订正：①#5「G-2 包 license」原记 A 已闭合**系错划**（义务含根与 `packages/*` 两面，实测仅 `apps/cli` 在位）→ 改判 C；②commit 数 31→**32**（漏计本卡第二笔 `6d17f20`）；③断言集「6 例」实为 **9 用例／6 组判据**。另 #4/#17 摘要含裸竖线（GFM 列错位，且整行 `includes` 断言对其零判别力）已转义，并把断言改为逐格解析。

## 顺带处理说明（卡边界核对）

- **不顺带清偿** M5/M6 §G 移交未落卡项（#22/#23 只登记不入实现），**未改任何冻结档案**（plan\ 下 T0–T2 档案与 M6.md 零触碰）。
- 本卡产品仓改动仅限：新增 `docs/milestones/M7.md` 内容、`docs/dev/seams.md` 追加与两行订正、`apps/cli/test/wp14-milestone.test.ts` 新增、三处纯注释/文档补注（#2/#3）。**无 src 行为改动**。
