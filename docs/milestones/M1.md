# M1 最小会话 — DoD 勾验表

> 复制自 `D:\StandardCode\StandardCode_v2.8.md` §2（2026-09-07，WP-07 迁移，逐字保留，原文为准）。

| M | 目标 | 范围（对应条目） | DoD（唯一验收标准，B-09） |
| :-- | :-- | :-- | :-- |
| **M1 最小会话** | 一条能跑通的生命周期 | **L5 双协议（Anthropic Messages + OpenAI Chat Completions 最小实现，PROD-010——无 Provider 则主循环无法验收）**、L1 主循环（循环+状态对象，ARCH-005）、L0 输入四模式、最小命令集（/help /clear /exit /model /permission）、六工具（Bash/Read/Write/Edit/Glob/Grep）、权限仲裁 4 模式、guard-path 常驻、prompt-layout 最小分区（CTX-100）、cacheScope 从第一天生、token usage 四列流式累计（CTX-102） | E2E 边界场景 ③④ 通过；协议不变量断言开启；Golden 快照第一周建立 |

## 勾验表

- [x] E2E 边界场景 ③④ 通过（apps/cli/test/e2e-scenarios.test.ts：③ tool/stream 双相中断+合成 error tool_result+树清理时序+resume 等价；④ 畸形连败 3 次预算耗尽 fail-closed、无悬空 tool_use——2026-09-08，WP-13）
- [x] 协议不变量断言开启（每 turn tool_use↔tool_result 配对断言默认开，WP-02；E2E 全程运行且无不变量异常——WP-13）
- [x] Golden 快照第一周建立（evals/golden/ 采集+对比+首基线，2026-09-08，WP-10；"第一周"按 B-08 范围切片理解为 WP-04 分区稳定后立即 [自定]）
