# M6 高阶编排（实验子项目） — DoD 勾验表

> 复制自 `D:\StandardCode\StandardCode_v2.8.md` §2（2026-09-07，WP-07 迁移，逐字保留，原文为准）。

| M | 目标 | 范围（对应条目） | DoD（唯一验收标准，B-09） |
| :-- | :-- | :-- | :-- |
| **M6 高阶编排**（实验子项目） | 复杂任务形态 | Agent Teams、workflow（ORC-023/024，默认关闭 feature flag）、`/workflows` `/batch` `/loop` `/btw` `/branch` `/fork` `/export` | workflow vm 沙箱逃逸测试通过；teams 消息泵五事件齐 |

## 勾验表

- [x] workflow vm 沙箱逃逸测试通过 — 载体=**WP-02**（ace751a 实现＋667ad73 修复，V 复验核销 DoD①-⑦ 全 ✓，CI gate 35304648447 三平台 success）：`packages/capabilities/src/workflow/sandbox.ts`＝vm context（`Object.create(null)` null 原型＋`codeGeneration{strings:false,wasm:false}`＋context 内守卫 prelude 禁 `Date.now()`/`Math.random()`/无参 `new Date()`）＋宿主函数 context 内桥接器＋JSON 克隆＋`toContextError`（同步 throw／Promise onRejected 双路宿主错误归一）；逃逸测试集（wp02 四套 **74 例**）：原型链/`eval`/`new Function`/WASM 四类拒绝＋三禁各一例抛错＋`require`/`process`/`fs`/`module`/`Buffer`/`global` 六名不可达＋**四通路全 BLOCKED**（成功值/Promise 本体/同步 throw/异步 rejection 的 `err.constructor.constructor("return process")()` 一律 `EvalError`，实害探针读宿主 fs 失效）；缺口存在性对照 A1（自建 context 不装桥接器）仍 `LEAK`＝封堵真实。变异七针 11/2/2/2/3/1/16 红、无 0 红针（V 复验节）。
- [x] teams 消息泵五事件齐 — 载体=**WP-06**（d70f2f4，V 核销 DoD①-⑥ 全 ✓，CI gate 35302605655 三平台 success）：`packages/capabilities/src/teams/events.ts` in-process 消息泵五事件逐枚一例＝`shutdown_request`（转 user turn 由模型决定、**不自动终止**）/`new_message`（`from==="user"` 直传，否则渲染 `{from,text,color,summary}`）/`new_messages`（批量积压合成**单条** user turn）/`aborted`/`idle_timeout`（一次性通知）；未知事件型抛错禁静默。V 独立探针直驱复核（不采信自报）＋变异四针各精确红；投递语义＝每次投递均为 `role:"user"` 单文本块 turn 由下一轮消费。

## CI 对账记录（gh run list/view，2026-09-19，WP-12 逐项）

M6 全部 **17 commit**（`git rev-list --count M5..HEAD`=17 实测；M5 冻结 tag=d75b2de，不含本收口 commit）→ run 全枚举对账（gh run list createdAt≥2026-09-18 枚举得 **12 runs**；5 个 commit 与同卡后续 commit 同批推送、由同批 head commit 的 run 承载无独立 run——gh 全枚举其 headSha 零命中证无漏无多）：

| 卡 | commit → CI run | 结论 |
| :-- | :-- | :-- |
| WP-01 | 8489f8f → 35297987289 | success |
| WP-02 | ace751a → 35301519627；667ad73 → 35304648447 | success ×2（V 退回 R1 宿主错误逃逸＋R2 判别力→修复→复验核销） |
| WP-06 | d70f2f4 → 35302605655 | success |
| WP-03 | 5c9436b＋ccfe5ae（同批＝初版＋main 抽查补齐）→ 35310902451 | success |
| WP-04 | ae0d0e6 → 35318664046 | success |
| WP-05 | 785d4be → 35326713227 **failure**（ProviderAdapter 误导入 capabilities）→ b8cc05b → 35326963934 success | failure ×1 卡内即修复转绿（3 分钟后类型修复 commit 推送） |
| WP-07 | 0a3d54b＋e65f5ca（同批）→ 35381012445 | success（fix＝resolve 契约断裂修复） |
| WP-08 | 710ec82＋73a8611（同批＝实现＋X-2 补判别用例）→ 35381012445 | success（同 run 承载 0a3d54b/e65f5ca/710ec82/73a8611 四 commit） |
| WP-09 | eb8d01e → 35392757556 | success |
| WP-10 | c729e36＋7b10c06（同批＝实现＋R1 i18n 修复）→ 35401745247 | success |
| WP-11 | 66d1fe9 → 35436781275 | success |

**HEAD（66d1fe9）run 35436781275 步骤级逐项**（gh run view --json jobs 实测）：gate job（ubuntu/macos/windows）三平台 conclusion=success、headSha `66d1fe9dee723eeb867bbccb4b7255faf105f596` 全字；每 job 内 install / install ripgrep / version gate / cold-start gate / typecheck gate / rust build / test gate / rust fmt gate / rust clippy gate / rust test gate 十步全 success（`verify bwrap usable`=Linux 条件步：ubuntu=success、Windows/macOS=skipped，设计预期）。收口 commit 由 main 推送后触发 run，run 号回填随 main 执行（M4 e612002 先例）。

**对账说明**：failure 1 处（35326713227）系 WP-05 实现初版 ProviderAdapter 导入错误，与修复 commit b8cc05b 构成「卡内即修复转绿」对（M4/M5 CI 对账先例同形；该 failure run 此前未入结果页/流水，本次对账全枚举补登）。

## 其余收口项指针（DoD③④⑤⑥，明细在 M6-1-results §WP-12）

- **DoD③ 接缝登记**：`docs/dev/seams.md` 追加 **⑰⑱⑲⑳㉑ 五条**，各=定义+锚点+测试三列（追加式，既有 81 行基线逐字节验证无损、既有行零触碰）。
- **DoD④ 断言集**：`apps/cli/test/wp12-milestone.test.ts`（新，4 例）＝①命令全集仍 30 件且集合 ≡ §8.2 M1∪M2∪M3∪M4（M6 零新增）②postinstall 零生命周期脚本负查（ADR-0044）③M6 新增面零注册（基表对 §8.2 行 365 七件候选零命中＋工具面恰六件不含 SendMessage/Workflow）④实验门候选面守恒（`experimentalCommandNames()` 恰 7 件≡行 365、缺省关＝活跃放行面 []）。与 M4 `wp12-command-set`/M5 `wp10-milestone` 同源互补（本卡=收口时点零增量复核+M6 新增面负查）。
- **DoD⑤ 偏差汇总遍**：程序化计数（node「连续递增序号存在性」口径＋.work 报告复核，2026-09-19）：M6 十一卡偏差 **99 条**（按 WP-01→11 序 **9 / 10〔8+修复节 2〕 / 8 / 12 / 6 / 6 / 10 / 7 / 14 / 12〔10+D-1/D-2〕 / 5**），与逐卡板卡/结果页声称数 **一致 11/11**（WP-09 14 条＝`.work/wp09-x-report.md` §6 表 A1-A14 实数；WP-10 十条＝`.work/wp10-x-report.md` §八 1-10 实数）；`[自定]` 标注出现数=**31**（同序 7/2/4/1/1/3/6/1/4/1/1，限定交付物/过程要点/偏差登记三类条目行口径，M5 WP-10 同口径）供 G。
- **DoD⑥ 全量**：全量套件与八包 tsc 0 实测见结果页 §WP-12 测试输出尾部。

## M6 卫生顺带处理表（逐条做/不做留痕；卡边界=顺带义务非 DoD，不含 M5 §G 移交八项——见板头处置，登记仪表盘 3.2 与 G 门复核）

> 来源=M6 各卡结果页/板卡登记的观察（O）项与未解决/未接线（F）项。处置口径：仅在既有卡已顺带清偿时记「已做（卡号）」，其余记「不做」+理由（B-03 不提前实现/接线义务归 G 门 form 化或后续卡/观察无害留观/登记级小疵）。M6 新增面接线义务的 G form 化候选=「M6 统一接线义务」（runsDir 注入/scriptPath 回填/配方投递/真实 runner/agent loop 四注入面/per-worker 转录生产者/loop 灌回/UI 确认路径/桥接装配位等）。

| # | 来源 | 摘要 | 处置 | 理由 |
| :-- | :-- | :-- | :-- | :-- |
| 1 | WP-01 未解决① | 接缝㉑ 遥测零事件面 | **已做（WP-11）** | 桥接面+八枚事件+门序断言落地（§WP-11 核销） |
| 2 | WP-01 未解决② | experimental 告警未入 /doctor 汇聚 | 不做 | 启动 notice 为唯一通道（无害留观；可随 /doctor 后续扩展） |
| 3 | WP-01 未解决③ | M7 四件居门拒绝面 | 不做 | M7 落地时一行迁移（gate 模块头注已登记迁移点） |
| 4 | WP-02 未解决① | timers 注入与异步段超时 | 不做 | 异步段归 budget/abort 治理（WP-03/04 面）；timers 注入=新增逃逸面不引入 |
| 5 | WP-02 未解决② | `workflow()` 父层真实现 | **已做（WP-03）** | 编排内核落地父层编排（sandbox 内 `workflow()` 槽位由内核消费） |
| 6 | WP-02 未解决③ | CJK meta.name 净化归一缺省名 | 不做 | script-store 测试锁定该行为（形制面，无害） |
| 7 | WP-03 未解决① | 真实安全分类器能力缺位（默认放行） | 不做 | 接线点+默认放行已核销；真分类待宿主装配（后续里程碑义务） |
| 8 | WP-03 未解决② | 跨 workflow+主会话统一 20 上限 | 不做 | 宿主装配层义务，另立卡（B-03） |
| 9 | WP-03 未解决③ | hooks-engine flaky | 不做 | 负载族逐文件单跑绿（既有 flaky 候选留观） |
| 10 | WP-04 未解决① | 装配未接线（runsDir 注入/scriptPath 回填/配方投递） | 不做 | WP-02/03/04/05 共性欠账，**G 门 form 化为 M6 统一接线义务** |
| 11 | WP-04 未解决② | 无 `Workflow` 工具本体（DoD③ 只验字符串形） | 不做 | 同上（接线义务） |
| 12 | WP-04 未解决③ | `[CC]` 未定位项（Pe 哈希/resultPreview/F() 转义） | 不做 | `attempts` 语义经 V 订正已定位且实现一致；余项报告 §12 未解 |
| 13 | WP-04 未解决④ | 遥测仅本地回调 | **已做（WP-11）** | telemetry-bridge 落地（tengu→sc 映射+facade sink 实收三例） |
| 14 | WP-04 未解决⑤ | 同 run 并发写未校验/agentId 一致性 | 不做 | 实测 started×3 共用末次结果（登记留观，实务窄场景） |
| 15 | WP-04 未解决⑥ | journal 无 fsync | 不做 | 进程硬杀依赖逐条 append 落盘（登记） |
| 16 | WP-04 O1 | 坏行跳过×失败门交互（回放陈旧 result） | 不做 | 修法与用例归后续卡（仪表盘 3.2 已登记） |
| 17 | WP-04 O2 | 配方换行/回车未转义（非合法字面量） | 不做 | 同上 |
| 18 | WP-04 O3 | 套件缺 parallel/pipeline 端到端用例 | 不做 | V/main 真内核探针已补测覆盖成立；套件用例归后续卡 |
| 19 | WP-04 O4 | 回放不落新记录致 `resumeFromRunId` 链断 | 不做 | 正确用法=指回最初派生 run（登记） |
| 20 | WP-04 O5 | 键派生 JSON 域外非单射 | 不做 | 纯 JSON 值分辨正常；非 JSON 值与沙箱 clone 同形（实务难达） |
| 21 | WP-05 未解决① | 真实 runner 未接线（注入链未触发/树只反映回放） | 不做 | M6 统一接线义务随 G form 化 |
| 22 | WP-05 未解决② | 主循环读取 workflow 返回值未实现 | 不做 | 同族（接线义务） |
| 23 | WP-05 未解决③ | 看板无生命周期清理调用方 | 不做 | `unregister` 零生产调用者（已结束 run 树持续可见至进程结束，无害） |
| 24 | WP-05 未解决④ | 非 REPL 路径无 drain 点 | 不做 | 同族（接线义务） |
| 25 | WP-05 O1 | `snapshot()` agents 数组浅拷（篡改快照污染内部） | 不做 | 修法一行、severity 低（树渲染只读），归后续卡 |
| 26 | WP-05 O2 | 门内实现合并顺序断言缺口（针 e 0 红） | 不做 | 判别力缺口随核销登记，「合并顺序断言」用例归后续卡 |
| 27 | WP-05 O3 | phase index 基数锚点未钉（1-based） | 不做 | [自定] 已登记（O3 复核成立） |
| 28 | WP-06 O1 | shutdown 渲染未走 `formatTeammateMessage` | **已做（WP-07）** | O1 收敛：改走 formatTeammateMessage（wp06 断言零改动仍绿） |
| 29 | WP-06 O2 | `inputSchema.message` 声明 object 与 union 不符 | **已做（WP-07）** | O2 收敛：改 `anyOf[string,object]` |
| 30 | WP-06 O3 | 测试标题 `DoD①①` 笔误 | 不做 | 文案级 |
| 31 | WP-07 O4 | `to` 描述 `[ref]` 后缀未实现 | 不做 | 依赖 §12 未解④ Os 名录展示面（随 WP-09 通讯录消费面复核，登记） |
| 32 | WP-08 F1 | 运行器四注入面（deliverToModel/deliverToMain/terminate/requestApproval）接入 agent loop | 不做 | M6 统一接线义务（候选 M7） |
| 33 | WP-08 F2 | UI 确认路径（A 级 §6.3 L158740） | 不做 | 无 UI 层（M7/UI 面） |
| 34 | WP-08 F3 | 不做 plan mode 本体 | 不做 | 卡边界（M1/M3 既有 Plan 面，本卡只接协议） |
| 35 | WP-08 F4 | 未改既有权限确认链序 | 不做 | M4 已核销面，未触碰（要求本身） |
| 36 | WP-08 O1 | `port.send` 返回值丢弃（响应落空且无告警） | 不做 | 登记留观（归接线义务一并复议） |
| 37 | WP-08 O2 | `requestApproval` 抛错→`receive()` reject 未转告警 | 不做 | 登记留观 |
| 38 | WP-08 O3 | 幂等游标仅进程内存（重启后重读全 inbox） | 不做 | 与 WP-04 O4 同族（登记） |
| 39 | WP-08 O4/O5 | 锚点小疵（ORC-050 实锚行 298 非 299；`shutdown_rejected` 实锚 L56524 范围略窄） | 不做 | 登记级（D-3 勘误沿用已补录） |
| 40 | WP-09 F1 | per-worker 转录生产者缺位 | 不做 | M6 统一接线义务 |
| 41 | WP-09 F2 | 恢复种子灌回 agent loop 续跑未接线 | 不做 | 同族；若落可一并复议 D-1（task_assignment 通知） |
| 42 | WP-09 F3 | CLI /tasks 未展示 owner/blockedBy 列 | 不做 | 守「不改面板语义」（BLK-08=①） |
| 43 | WP-09 F4 | 零命令/工具增量 | 不做 | 要求本身（CLI_COMMANDS 30 恒等=收口断言①在岗），非缺口 |
| 44 | WP-09 F5 | `createSharedTaskBoard` 无生产消费点 | 不做 | V 补登记（接线义务族） |
| 45 | WP-09 D-1/D-2/D-V1/D-V2 | 不移植 task_assignment 通知（接缝⑲ 从严）/引句实位 L208382/L179075 归属 §9/`send-message.ts:3` 注释行 298 | 不做 | D-1=裁定成立从严；余=登记级（D-V2 随后续卡顺手订正） |
| 46 | WP-10 D-1 | harness deny 段仅查显式 subagentType（缺省类型×deny 在 /fork 与 /subtask 两通道同不设防） | 不做 | M3 既有语义（V 亲读+main 亲对一致），非本卡引入；「缺省类型也过 deny」属规格变更归 G/后续里程碑 |
| 47 | WP-10 R1 | 任务书约束须按守卫扫描面精确划界（i18n 八键教训） | 不做 | 流程教训已入档仪表盘 3.3（非产品面） |
| 48 | WP-11 F1 | 桥接生产装配位未接线（无 runner 生产构造点） | 不做 | M6 统一接线义务（装配契约=progress.ts 行 296 注） |
| 49 | WP-11 F2 | teams 扩位两码产生点无 teammate 生产 spawn 面 | 不做 | 发射面=契约+API（M5 model_fallback_triggered 先例同形） |

**小结**：49 项＝已做 **5**（#1/#5/#13/#28/#29，均为既有卡顺带清偿的追溯认定）＋不做 **44**（其中「M6 统一接线义务」族建议 G 门 form 化）。
