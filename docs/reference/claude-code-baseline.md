# Claude Code 逆向参考基线（指针版）

> 复制自 `D:\StandardCode\StandardCode_v2.8.md` 附录 F（2026-09-07，WP-07 迁移，逐字保留，原文为准；本文件随实现演进）。

## 附录 F · Claude Code 逆向参考基线（指针版）

1. **三份互补产物**：代码层（`evidence/claude-src-extracted/`，1573 模块=837 `_N.js`+562 chunk+具名；`_440.js` 331,190 行）、提示词模板层（`system-prompts/` 内 732 模块，实测精确命中；全仓另有 3 个仓库自述文件）、运行时快照（`prompt-capture/extracted/`，27 工具单会话）。三者拼合缺一不全（ADR-010）。
2. **不可复用双重成立**：授权 + 混淆（4 道不可逆损失、无 source map）。参考思路/结构/措辞完全可行，逐字抄写无价值且违规。
3. **关键模块指针**（美化版行号）：主循环 `_440.js:149307`；组装 `nz`:266805 / `iWt`:267474 / `e$s`:273289；compact 常量 `:132212-132471`；权限仲裁 `STo` `_440.js:61596-61604`、规则引擎 `chunk-4svxqcrq.js`、模式层 `_50.js`、hook 决策封装 `_83.js`（dig-02）；hooks 引擎 `_440.js`（`_184.js` 为 REPL 渲染层，dig-04）；MCP 客户端 `_440.js`（v1/v2 双代，dig-05；云控制面 `_13.js`/`_660`）；Edit/Bash 实现主体 `_440.js`、`_525.js`（rg/WebFetch/Read 描述，dig-03）；模型目录 `_695.js`；env `_811.js`；记忆常量 `_533.js`；错误码 `_795.js:11`；file-history `_788.js`。
4. **证据可靠性声明**：六篇报告+dig 系列 2026-09-04~05 全量独立核验，约 360 组抽验，9 处幻觉级修正（登记于 `README.md` §5 勘误登记）；引用行号前对照勘误表。
